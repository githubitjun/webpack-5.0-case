
/**  =========================> *|`| % moduleName % |`|*<======================== **/

// methodName
// import * as types from '../mutation-type';

// api
// import {
//     functionName,
// } from '/url';

const config = {
    state: {
        // saveData: {}
    },
    mutations: {
        // [types](state, saveInfo) {
        //     state.saveData = saveInfo;
        // }
    },
    actions: {

        // 获取信息
        // FunctionName({ commit }, queryData) {
        //     if (!queryData) {
        //         queryData = {};
        //     }
        //     return new Promise((resolve, reject) => {
        //         functionName(queryData).then(response => {
        //             if (response.data.status === 1) {
        //                 commit(types, response.data);
        //             }
        //             resolve(response);
        //         }).catch(error => {
        //             reject(error);
        //         })
        //     })
        // }
    }
};

export default config;

